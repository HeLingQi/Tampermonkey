# Changelog

## 0.5.7

- 修复当前用户黑/白名单菜单没有状态判断、四个互斥动作同时显示的问题
- 增加 `GM_unregisterMenuCommand`，根据当前主页 UID 动态重建菜单
- 白名单仅显示当前可执行的“加入”或“移出”其中一项
- 黑名单仅显示当前可执行的“加入”或“解除”其中一项
- 已在对应名单时菜单增加 `✓` 状态标记
- SPA 切换用户主页、加入/移出名单、同步官方黑名单后立即刷新菜单状态
- 离开用户主页后自动清除当前用户相关菜单，避免误操作上一个账号

## 0.5.6

- 增加黑名单/白名单维护功能，支持多行 UID 批量编辑
- 白名单与黑名单改为互斥状态，白名单优先级最高
- 加入白名单时，如用户已被微博实际拉黑，会调用 `/ajax/statuses/deleteFilters` 自动解除拉黑
- 加入白名单同时清理插件本地黑名单、自动拉黑记录及对应种子，解决“白名单但主页显示已拉黑”冲突
- 加入黑名单时自动从白名单移除，并同步微博官方黑名单
- 增加当前用户快速加入/移出白名单、加入/解除黑名单菜单
- 白名单用户按钮显示“白名单”而非“已拉黑”

## 0.5.5

- 修复刷新个人主页时“拉黑”按钮先出现在页面右上角、随后跳回正确位置的问题
- 移除 `.wflp-float` 临时兜底；未找到微博原生操作区时暂不渲染，等待 DOM 稳定后再注入
- 增加“已关注用户不自动拉黑”开关，默认开启
- 已关注用户默认跳过关注/粉丝 10 倍规则和 Risk Score 自动拉黑
- 手动拉黑按钮仍保留，不受该保护开关影响

## 0.5.4

- 调整个人主页 `.wflp-profile` 按钮尺寸：宽 `77px`、高 `33.14px`
- 调整个人主页按钮字号为 `.875rem`
- 保持橙色描边胶囊风格，并将水平 padding 归零以确保精确尺寸

## 0.5.3

- 新增关注/粉丝比例自动拉黑规则
- 当 `关注数 > 粉丝数 × 10` 时直接拉黑当前用户，不再扫描关注列表
- 比例规则优先级高于 Risk Score 规则，且不依赖种子库是否为空
- 从微博资料接口读取 `friends_count` / `followers_count`；数据不可靠时跳过比例判断
- 粉丝为 `0` 且关注数大于 `0` 时按字面规则视为命中
- 比例规则触发的自动拉黑不会加入种子库，避免递归扩散
- 判定历史记录自动拉黑原因、关注数、粉丝数和比例

## 0.5.2

- 增加当前账号真实拉黑状态索引，已拉黑用户显示“已拉黑”禁用态
- 手动拉黑成功后，同 UID 在当前页面上的所有按钮同步刷新状态
- 官方黑名单后台周期同步，刷新真实拉黑状态
- 将“当前账号已拉黑”与“种子黑名单”拆分，导入种子不再误显示为已拉黑
- 已拉黑用户不会再次执行关注列表扫描或重复调用拉黑接口
- 拉黑按钮统一为微博橙色描边、白底、胶囊圆角风格
- 主页、评论区、关注列表按钮统一状态和视觉样式

## 0.5.1

- 修复个人主页因异步竞态导致重复注入多个“拉黑”按钮的问题
- 个人主页按钮增加注入锁、UID 去重和路由切换清理
- 支持 `https://weibo.com/u/page/follow/<uid>` 关注列表页
- 在关注列表中为每个可识别用户注入一键“拉黑”按钮
- 关注列表动态加载时自动补齐按钮
- 关注列表手动拉黑同样会加入持久种子库

## 0.5.0

- 在微博个人主页注入一键拉黑按钮
- 在微博评论区注入一键拉黑按钮
- 支持动态加载评论自动补齐按钮
- 手动拉黑同时加入持久种子库
- 自动拉黑仍保持非递归，不加入种子库
- 后台静默扫描，不影响页面阅读
- 使用自定义 Toast / Modal，移除原生弹窗
- 支持种子黑名单 JSON 导入/导出
- 支持 Risk Score、重点种子权重、白名单和安全缓存
- 增加 GitHub `@updateURL` / `@downloadURL` 自动更新地址
